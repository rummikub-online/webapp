export default defineAppConfig({
  ui: {
    notifications: {
      position: "max-md:top-0 max-md:bottom-auto",
    },
    notification: {
      ring: "ring-0",
      shadow: "shadow-md",
      progress: { base: "h-0" },
    },
  },
});
